{
  "name": "land",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps_vezham/land/src",
  "targets": {
    "config-local-hostile": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps_vezham/land",
        "commands": [
          "sudo pnpm exec hostile set localhost land.vezham.local && sudo pnpm exec hostile set localhost land.mock.vezham.local"
        ],
        "parallel": false
      }
    }
  },
  "tags": ["category:vezham"]
}
