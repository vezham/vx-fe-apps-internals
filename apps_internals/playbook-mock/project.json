{
  "name": "playbook-mock",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps_internals/playbook-mock/src",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps_internals/playbook-mock",
        "commands": ["pnpm serve"],
        "parallel": false
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps_internals/playbook-mock",
        "commands": ["pnpm build"],
        "parallel": false
      }
    },
    "generate": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps_internals/playbook-mock",
        "commands": ["pnpm generate"],
        "parallel": false
      }
    }
  },
  "tags": ["category:internal", "mock"]
}
